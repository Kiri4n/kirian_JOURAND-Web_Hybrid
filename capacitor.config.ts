import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kirian_JOURAND',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
